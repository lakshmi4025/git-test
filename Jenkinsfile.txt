pipeline {
    agent test

    stages {
        stage('Build') {
            agent { label 'test​' }
            steps {
                echo 'Building..'
                sh '''
                '''
            }
        }
    }

    post {
        success {
            echo 'This will run only if successful'
        }
    }
}
